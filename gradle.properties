org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Backend API base URL for local development.
# Emulator: http://10.0.2.2:3000 (host machine)
# Real device on same network: http://<your-machine-ip>:3000
# Leave blank to disable remote sync (offline mock only).
# Override per-build: ./gradlew assembleDebug -PapiBaseUrl=https://your-host
apiBaseUrl=http://10.0.2.2:3000
